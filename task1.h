#pragma once

// reads the string entered by the user
char* readString(int* const errorCode);

// converting a number to the smallest of its digits
char* transformation(const char* const string, int* const errorCode);
