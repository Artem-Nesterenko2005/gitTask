#pragma once

// read data from file
char* readFile(int* const errorCode, const char* const fileName);

// removing identical consecutive elements
void stringProcessing(int* const errorCode, const char* const string);
